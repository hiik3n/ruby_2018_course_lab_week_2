<div class="card mb-4 shadow-sm">
  <div class="card-header">
    <h4 class="my-0 font-weight-normal"><%= user.name %></h4>
  </div>
  <div class="card-body">
    <img src="<%= user.image_url %>" class="rounded mx-auto d-block" alt="...">
    <a role="button" class="btn btn-lg btn-block btn-outline-primary mt-3">Show</a>
  </div>
</div>
